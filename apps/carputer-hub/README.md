# Carputer Hub Skeleton

This directory currently holds a minimalist hub implementation used to validate the Buildroot package pipeline.

The binary logs system information and exposes a simple IPC-ready directory layout. Replace this stub with the production hub code when available.
